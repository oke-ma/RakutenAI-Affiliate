# CLAUDE.md

このファイルは、RakutenAI-Affiliateプロジェクトでの作業時にClaude Codeが従うべき指示を定めるものです。

## 扱わないジャンル・商品(除外ルール)

**災害支援・返礼品のない寄付は、このプロジェクトの対象から完全に除外する。**

理由: ①アフィリエイト報酬が通常の商品と同様に発生するか不透明、②被災地支援を紹介して収益を得る構図には印象面のリスクがある。

- ジャンルIDでの絞り込み時(例: 災害支援・サービス genreId 101438)だけでなく、**他ジャンル内に返礼品なしの寄付型商品が混ざっている場合も、選定対象から除外すること**。
- 商品調査・選定の段階で、対象商品が「返礼品なし」の寄付型でないかを確認すること。

## 市場調査・マーケティングリサーチ実施ルール

このプロジェクトで需要分析・戦略判断を行う際は、以下を必ず満たすこと。数字を集計するだけでは市場調査は完了していない。

1. 「何が人気か」だけでなく「なぜ人気か」を調べる。理由を明示できないランキング・数値は判断根拠として使わない。
2. その理由が「現時点でも有効か」を確認する。過去の記事・データを引用する際は、日付を確認し、状況が変わっていないか最新情報で裏取りする(価格・規制・トレンドは変化する)。
3. データの出典が、実際に収益化する対象のプラットフォーム(このプロジェクトでは楽天)のものか確認する。他プラットフォームのデータは参考程度に留め、意思決定の主根拠にしない。
4. サンプル数を明示する。2〜3件の観測で「実態はこうだ」と結論づけない。サンプルが少ない場合は、少ないことを正直に申告し、結論を留保する。
5. 実測データ(クリック数・成果等の一次データ)が既にある場合、それを軽視して、後から見つけた弱い状況証拠で安易に上書きしない。
6. 顧客の行動・心理(なぜクリックするか、なぜ避けるか)を、数値の裏側として必ず考える。

### 現在の適用例: リンク配置(本文かリプライか)【解決済み・履歴】

アフィリエイトリンクをツイート本文に含めずリプライに出す方針は、当初は外部の第三者情報(Xのアルゴリズムに関する報道等)に基づく暫定的な判断だった。**その後、実際にXへ投稿して検証した結果、アフィリエイトリンクを本文に直接含めてもXのカード(サムネイル付きリンクプレビュー)が正しく自動生成されることを一次データで確認できた**ため、運用ルール.md 5章を「各商品紹介ツイートの本文にリンクを直接含める」方式に更新済み。ルール5(実測データを優先する)が実際に機能した事例として記録しておく。

### 現在の適用例: ジャンルごとの消費者心理を都度確認する

食品(水産品・肉・米等)と、日用品・体験型返礼品とでは、消費者が重視するポイントが異なる(食品は味・鮮度・量・保存性、日用品は実用性・コスパ、体験型は特別感・限定性・思い出価値など)。魚介類・水産加工品向けに設計した紹介文の書き方(商品ページの実用的特徴を書く形式)を、ジャンルを変えてそのまま流用しないこと。新しいジャンルのスレッドを作る際は、その都度そのジャンル固有の消費者心理を確認してから紹介文を設計すること(ルール6参照)。

### 調査結果を実際の文章に反映させるルール

ジャンル固有の消費者心理調査(上記)で見つかった心理的な魅力(ブランドへの信頼、贈答需要、特別感など)は、必ず紹介文執筆の段階で、TOP5(または対象商品群)全体のどこかに具体的に反映されていることを確認すること。調査したのに、実際の紹介文が機能面(使いやすさ・保存性・調理の手軽さ)の説明だけで終わっている場合、それは調査結果を活かせていない状態とみなす。

紹介文完成後、**「調査で見つけた心理的魅力のうち、どれが実際の文章のどこに反映されているか」を一覧で照合し、反映されていないものがあれば書き直すこと**。反映させる際も、根拠の範囲を超えないルール(上記)は適用される(調査で見つかった魅力を、確認できていない商品にまで架空に広げて書き加えることは禁止)。

この確認は、精肉・肉加工品に限らず全ジャンルに適用する。

## 商品紹介文リサーチ実施ルール(情報源を尽くすルール)

このプロジェクトで商品紹介文を書く際、**ジャンルを問わず全ての投稿**に適用する。1つの情報源(商品ページの説明文)で十分な情報が得られなかった場合、それだけで「情報なし」と判断せず、以下を順に試すこと。

1. 商品ページ内の他のセクション(説明文以外の箇所: スペック表、Q&A、店舗からのメッセージ欄等)
2. 同じ店舗の他商品ページ、店舗トップページ
3. 楽天のレビュー一覧ページ(商品ページとは別にある場合)
4. Web検索(商品名・ブランド名での一般検索)

これらを順に試した上で、それでも情報が得られない場合のみ、簡潔な記述でよしとする。その際は、**「①〜④のどれを試して見つからなかったか」を具体的にfactSources(posts/*.mdのfrontmatter)または別途メモに残すこと**。「確認しましたが見つかりませんでした」という報告だけでは不十分で、何を確認したかを示すこと。

## 商品調査の必須手順(レビュー確認の実施基準)

**背景**: 魚介類・水産加工品のスレッドで、商品ページを確認しただけで「レビューも確認した」という扱いになり、実際にはレビューを数件程度、あるいは全く読み込まずに「(リピートする)理由は見当たらなかった」と結論づけていたことが発覚した。これは特定ジャンル・特定商品固有の問題ではなく、構造的な欠陥である。

**このプロセスに適用の起点・区切りはない。特定のジャンルや時点から始まるものではなく、このプロジェクトで生成する全てのツイート(過去に作成済みのもの・現在作業中のもの・将来生成する全てのもの)に、無条件で適用される。**

1. **商品ページの確認と、レビュー一覧ページの確認は、どちらも独立した必須ステップとする**(片方の確認をもう片方の代わりにしない。商品ページを見た=レビューも見た、という扱いは禁止)。
2. レビュー一覧は、要約や上位数件だけでなく、**最低10件程度、複数ページにわたって実際に目を通す**。
3. 紹介文を書く前に、実際に読んだレビューの中から見つかった具体的な記述を、**作業メモとして最低3つ書き出す**。このメモを残せない場合、「情報が見つからなかった」という結論を出すことを禁止する(メモが書けない=まだ十分に読んでいない、とみなす)。
4. この手順を怠ったまま「口コミによると」と書くこと、または「特に理由は見当たらなかった」と結論づけることは、ジャンルや時点を問わず禁止する。
5. **根拠の範囲を超えないルール**: レビュー等で確認できた対象・範囲を、言葉を言い換える際に不用意に広げないこと。例:「孫のために購入した」という1件の根拠から「家族への贈り物」のように対象を広げて書かない。使い方・効果・対象読者などを一般化する場合は、確認した件数・内容の範囲内にとどめる(「多く見られます」は複数件の独立した記述で裏付けられる場合のみ使い、1〜2件の場合は「という声も見られます」など、件数相応の表現にとどめる)。

## ルール検証の原則(代筆による検証を禁止する)

新しく追加したルール・手順が正しく機能するかを確認する際、答えとなる情報(調査結果・データ・文章の内容そのもの)を先に渡して、それを整形・反映させるだけの作業を「検証」として扱ってはならない。それは代筆であり、ルールが本当に機能する証明にはならない。

検証する際は、必ずAI自身に該当の調査プロセス(Web検索・ページ取得・情報収集)を実行させ、その実行結果(何を検索したか、どのURLにアクセスしたか、何件確認したか)を示させること。他者が既に見つけた答えと照合するのは、AI自身の調査結果が出そろった"後"に、答え合わせとして行う場合に限る。

検証結果を報告する際は、調査プロセス(URL・件数)を示すだけで終わらせず、**その根拠を反映した、修正後の実際の成果物(紹介文・投稿文など)まで必ずセットで示すこと**。調査したことの証明だけでなく、それが実際のアウトプットにどう反映されたかまで確認できて初めて、検証が完了したとみなす。

この原則は、商品調査に限らず、このプロジェクトで新しいルール・プロセスを追加した際の検証全般に適用する。

## スペックとベネフィットを区別するルール

このプロジェクトで商品紹介文を書く際、**ジャンルを問わず全ての投稿**に適用する。

- 客観的事実(スペック: 保存期間・内容量・成分・産地等)を書くだけで終わらせず、その事実が読者にとってどんな良いこと(ベネフィット)につながるかを、必ず1つ以上明示すること。「-20℃で24ヶ月保存可能」で終わらせず、「だから〜できる」という読者の生活・願望に接続する一文を加える。
- 「口コミによると〜という声が多い」も同様に、単に「リピートが多い」という事実で終わらせず、可能な限り"なぜ"リピートされているのか(味・食感・コスパ等、理由)まで踏み込んで書く。
- ベネフィットは、商品ページ・口コミ等で**実際に確認した事実から論理的に導けるものに限り、架空の設定は禁止**する(0章のルールと同様)。理由が確認できなかった場合は、無理に理由を付け足さず、「リピート購入が多い」という事実の提示にとどめる(架空の理由を書くくらいなら、理由なしの方が安全)。
- **ネガティブな言葉を避けて安心感を伝えるルール**: ポジティブな情報(対応の丁寧さ・品質保証など)を伝える際、その背景にあるネガティブな事象そのものを具体的に想起させる言葉(虫、腐敗、クレーム等)は使わないこと。同じ安心感を、ネガティブな言葉を使わずに表現できないか必ず検討する。例:「虫の発生時に対応してくれる案内があり安心」ではなく「万一の際のサポート案内も同封されており、安心して注文できます」のように、対応・保証の質の高さだけを伝える言い回しに言い換える。ただし、寄付の動機そのものを説明する文脈で必要な言葉(震災・被災地支援など)は対象外とする(置き換えると動機自体が伝わらなくなるため)。

## 投稿冒頭の見出しルール

**背景**: 定番・比較検討型スレッドで、1ツイート目が時事性の説明から始まっており、「これは何についてのスレッドか」が冒頭で分からない状態になっていたことが発覚した。特定のジャンル・特定の投稿固有の問題ではなく、構造的な欠陥である。

**このルールに適用の起点・区切りはない。定番・比較検討型か発見・驚き型かを問わず、過去に作成済みの投稿・現在作業中の投稿・将来生成する全てのジャンル・全ての投稿に、無条件で適用される。**

- 定番・比較検討型(6-1)の1ツイート目(リード文)の先頭には、「【楽天ふるさと納税】(ジャンル名) 人気返礼品TOP5」の形式で1行の見出しを入れる。
- 発見・驚き型(6-2)のフック文の先頭にも、原則として内容に応じた見出しを入れる。ただし単発ツイートは文字数制約が厳しいため、圧縮を試みても実効重み(280相当)に収まらない場合に限り、見出しを省略してよい。省略した場合は、その判断理由を記録に残すこと。
- 見出しを追加して文字数が140字を超える場合は、内容の削除ではなく圧縮を優先する。削らざるを得ない場合は、根拠が薄い部分から削ること(根拠の範囲を超えないルールと同様の優先順位)。

詳細なテンプレート・文例は [rules/運用ルール.md](rules/運用ルール.md) 6章を参照。

## 定番・比較検討型(6-1)の対象ジャンル・ローテーション

**背景**: 当初、対象ジャンルは楽天市場商品検索APIによる自社データ分析(主軸2ジャンル・準主軸4ジャンル)に基づく独自の絞り込みだった。この方針を改め、楽天ふるさと納税公式サイトの「ジャンル別ランキング」(event.rakuten.co.jp/furusato/special/)に準拠する形に変更した。

- 対象ジャンルは**16ジャンルで確定**する。**月1回、1日1ジャンルのペースでローテーションし、16日で1周する**運用とする。ジャンル名・ジャンルIDの正式なリストは [posts/rotation_genres.json](posts/rotation_genres.json)の`top5`配列(16件、ローテーション順)を参照する。**このJSONファイルが、TOP5・単発どちらのジャンルリストについても唯一の正式な情報源であり、CLAUDE.md内に同じ内容を重複して列挙しない。**

- 楽天ふるさと納税公式サイトの「ジャンル別ランキング」における下記の扱いを、このプロジェクトでも踏襲する。
  - ティッシュ・トイレットペーパー関連の細分化ランキングは、独立ジャンルとせず**日用品雑貨・文房具・手芸に含める**。
  - 楽天トラベルクーポンは、独立ジャンルとせず**施設利用・旅行・交通関連と統合する**(施設利用・旅行・交通関連は6-1の対象外だが、6-2等で扱う場合はこの統合ルールに従う)。
  - 「明日届く返礼品」は配送速度の切り口であってジャンルではないため、**ローテーション対象に含めない**。
  - **惣菜(うなぎ、他)は対象外とする**(楽天ジャンル検索APIでのジャンルID特定を試みたが確定作業を経て、最終的にローテーション対象16ジャンルには含めないことで確定した)。

## ローテーション運用ルール(rotation_state.json)

TOP5(6-1)・単発(6-2)それぞれの「次に使うジャンルの位置」は、[posts/rotation_state.json](posts/rotation_state.json)の2つのインデックスで管理する。

```json
{
  "top5_index": 0,
  "discovery_index": 0
}
```

- `top5_index`: [posts/rotation_genres.json](posts/rotation_genres.json)の`top5`配列(16件)内の、次に使うジャンルの位置(0始まり)。
- `discovery_index`: [posts/rotation_genres.json](posts/rotation_genres.json)の`discovery`配列(23件)内の、次に使うジャンルの位置(0始まり)。

### 単発の母集団を23ジャンルとした経緯

単発ローテーションの母集団は、**`products/rakuten_item_search.py`のGENRE_NAME_BY_ID(40ジャンル)を情報源として確定する**。理由: event.rakuten.co.jp/furusato/special/の「ジャンル別ランキング」(22種類)は意図的に絞られた別目的のリストであり、event.rakuten.co.jp/furusato/ホームの「ジャンルから探す」(20種類)はグループ見出しの中身が展開されずに数えられている可能性が高い。一方GENRE_NAME_BY_ID は、公式ジャンルページを実際に展開して確認済みであり、かつ本プロジェクトで実際にAPI検証済みのリストであるため、他の2出典より信頼できる。

当初「単発は39ジャンル」という想定を示していたが、これはTOP5で使う16ジャンルを除く計算を行っていなかった誤りであり、訂正した。GENRE_NAME_BY_ID(40ジャンル)から、①除外ルール対象の災害支援・サービス(1ジャンル)、②TOP5で使用する16ジャンル、の両方を除いた**23ジャンル**が、単発ローテーションの対象となる。確定した23ジャンルの実際のリストは[posts/rotation_genres.json](posts/rotation_genres.json)の`discovery`配列を参照(このJSONファイルが唯一の正式な情報源であり、CLAUDE.md内には重複して列挙しない)。

### 実行のたびの更新ルール

**このルールに適用の起点・区切りはない。今後生成する全てのTOP5・単発投稿に、無条件で適用される。**

- TOP5(6-1)を1件生成したら、`top5_index`を**1**進めて`rotation_state.json`を更新し、コミットする。リストの末尾(15)まで進んだら、次回は先頭(0)に戻る。
- 単発(6-2)は**1日2ジャンル、それぞれ2件ずつ(合計4件)生成する**仕様とする。1回の実行で`discovery_index`が指すジャンルと、その次のジャンルの計2ジャンルを使用し、実行後に`discovery_index`を**2**進めて`rotation_state.json`を更新し、コミットする。リストの末尾(22)を超える場合は、末尾まで使った後、先頭(0)に戻って続きのジャンルを使う(例: discovery_indexが22の場合、22番目と0番目の2ジャンルを使い、更新後のdiscovery_indexは1になる)。
- インデックスの更新・コミットは、実際にそのジャンルで投稿(draft)を生成し終えた後に行う。生成が完了しなかった(候補選定やレビュー確認の途中で終わった)場合は、インデックスを進めない。

## 毎朝のリサーチコマンド即答ルール

ユーザーが「今日のリサーチコマンドを教えて」(またはこれに類する依頼)をした場合は、以下の手順で**他の説明なしに、コマンドのみ**を即座に回答する。

1. [posts/rotation_state.json](posts/rotation_state.json)を読み、`top5_index`と`discovery_index`を確認する(この時点ではインデックスを更新しない。更新は実際に投稿を生成し終えた後、「実行のたびの更新ルール」に従って行う)。
2. [posts/rotation_genres.json](posts/rotation_genres.json)を読み、対象ジャンルを特定する。
   - TOP5用: `top5`配列の`top5_index`番目のジャンル、1件。
   - 単発用: `discovery`配列の`discovery_index`番目と、その次のジャンル、計2件(配列の末尾を超える場合は先頭に戻る)。
3. 各ジャンルについて、以下の形式でコマンドをそのままコピペ実行できる形で示す。

```
python products/rakuten_item_search.py --genre-id <ジャンルID> --keyword "ふるさと納税" --max-pages 3 --output-suffix <ジャンル名>
```

出力は上記コマンド3行(TOP5用1行+単発用2行)のみとし、リサーチ内容の説明・ジャンル名の解説・前置き挨拶などは付け加えない。

**このルールに適用の起点・区切りはない。今後、この依頼があるたびに無条件で適用される。**

## 発見・驚き型(6-2)は2案作成するルール

発見・驚き型(6-2)の単発ツイートを作成する際は、**1件ではなく必ず2件の候補を作成する**。同じジャンルのリサーチ結果から、それぞれ独立に「こんなのあるの?」度が高い異なる商品を選び、両方について商品ページとレビュー一覧(最低10件、商品調査の必須手順に準拠)を自力で確認したうえで、それぞれ完成した単発ツイートのdraftを作成すること。

- 2件の候補は、同一商品・同一店舗の色違い等ではなく、明確に異なる商品・異なる魅力軸(例: 価格の驚き/技術的な珍しさ/産地・製法の意外性 等)から選ぶことが望ましい。
- 2件とも、商品調査の必須手順・スペックとベネフィットを区別するルール・根拠の範囲を超えないルール・ネガティブな言葉を避けるルール・投稿冒頭の見出しルール等、CLAUDE.mdの全ルールを適用する(候補だからといって手順を省略しない)。
- どちらを実際に投稿するか(両方・片方のみ・どちらも不採用)はユーザーが判断するため、2案とも省略せず全文を提示すること。
- 定番・比較検討型(6-1)には適用しない(6-1は元々TOP5選定という複数商品比較の性質を持つため)。

**このルールに適用の起点・区切りはない。今後生成する全ジャンルの発見・驚き型投稿に、無条件で適用される。**

## コスト事故防止ルール

**このルールに適用の起点・区切りはない。今後の全てのセッション・Routine実行に、無条件で適用される。**

### 再実行前の重複防止ルール

Routineやセッションを新規に開始する際、作業に着手する前にまず`git status`と`git log`を確認し、前回の実行による「ローカルにはあるがプッシュされていないコミット」が存在しないか確認すること。存在する場合は、生成作業をゼロからやり直さず、**そのコミットをプッシュすることを最優先で試みる**。

### 並列実行の抑制

商品調査・レビュー確認を行う際、多数のサブエージェントを同時並行で起動することは、正当な理由(明確な時間制約等)がない限り避け、必要な範囲で**順次(逐次)処理を基本とする**。

関連ルールは [rules/運用ルール.md](rules/運用ルール.md) を参照(発信ジャンル・NGワード・アフィリエイト表記・コンテンツの型など)。
